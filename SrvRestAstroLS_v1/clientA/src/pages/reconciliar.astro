---
import Layout from '../../layouts/Layout.astro';
import ReconciliarApp from "../../components/agui/ReconciliarApp.svelte";

const canonicalHref = new URL(Astro.url.pathname, Astro.site).href;
---

<Layout title="Concilia | Reconciliar" theme_daisyui="cupcake" canonicalUrl={canonicalHref}>
  <div class="container mx-auto px-4 mt-2">
    <h1 class="text-xl font-semibold">🧾 Reconciliar</h1>
    <p class="text-sm opacity-70">
      Nueva ruta de trabajo seguro. Inicialmente reusa el flujo actual y luego lo vamos diferenciando.
    </p>
    <ReconciliarApp client:load />
  </div>
</Layout>

